# Hermes / Cursor Rules — min_agent

你正在维护一个中文注释风格的 AI Agent 学习项目。

## 项目定位

- 这是基于原生 tool_use / function calling 的 ReAct Agent。
- 禁止用正则或文本解析 LLM 输出提取工具调用。
- LLM 返回 JSON tool_calls，框架执行工具并把 tool role 结果喂回模型。

## 代码风格

- 文件顶部使用 `from __future__ import annotations`。
- 注释和文档字符串使用中文；变量名、函数名、类名使用英文。
- 使用 section banner 分组：`# ═══════════════════════════════════════════════════════════════`。
- 禁止隐式字符串拼接，连续 f-string 必须用 `+` 显式连接。
- 公共函数和方法必须写完整类型注解，包括 `-> None`。

## 类型规范

优先使用 TypedDict 描述跨模块的数据结构：

- `agent/llm.py`
  - `OpenAIConfig`
  - `ParsedToolCall`
  - `LLMResponse`
- `agent/tools.py`
  - `ToolResult`
- `agent/memory.py`
  - `MemoryEntry`
  - `MemoryResult`

不要在公共接口上使用裸 `dict` 或裸 `Callable`。
`Callable` 必须带参数，例如 `Callable[..., object]` 或 `Callable[..., str]`。

## Tool 系统规则

- 所有工具必须通过 `ToolRegistry` 注册。
- 工具函数可以直接 raise，`Tool.execute()` 会统一捕获并返回 `ToolResult`。
- 工具返回值会被 `str(output)` 转成字符串。
- 修改工具实现时，必须同步检查 JSON Schema 的 `description` 和参数是否准确。
- description 要说明"何时使用"，不要只描述实现细节。

## Memory 系统规则

- LLM 决定是否写入 memory；程序只做基础设施。
- `MemoryStore` 不应加入语义判断或自动总结逻辑。
- memory 分层：
  - target: `memory` / `user`
  - scope: `user` / `project` / `local`
  - topic: 主题名
  - pinned: 是否进入启动 prompt
- pinned 条目短小稳定；长细节用 `pinned=False`，按需 search/read。

## 禁止事项

- 不要硬编码 API Key。
- 不要绕过 `Tool.execute()` 直接调用工具函数。
- 不要把大段原始日志写入 pinned memory。
- 不要在 memory 中保存 prompt injection 指令。
